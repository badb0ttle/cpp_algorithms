/*blank*/
#include <bits/stdc++.h>

using i64 = long long;
const int mx=2e5+1;
int n;
int a[3][mx];
void work()
{
    std::cin>>n;
    for(int i=1;i<=n;++i)std::cin>>a[1][i];
    for(int i=1;i<=n;++i)std::cin>>a[2][i];
    for(int i=1)
}
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int _=1;
    std::cin>>_;
    while(_--)work();
    return 0;
}